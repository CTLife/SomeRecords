1. Install CentOS 6.

2. Install Anaconda3：
　　bash Anaconda3-4.4.0-Linux-x86_64.sh　　　
　　logout
　　which python
　　which pip
　　python -V
  　
3. Change name for python3 in "/home/pengyong/anaconda3/bin/":
   pip to pip3
   delete python (symbol link)
   canda to conda3
   anaconda to anaconda3
   which python
   which python3
   which pip
   which pip3
   which conda
   which conda3
   which anaconda3
   which anaconda
   change the first line of file "conda3" and "anaconda3", python to python3
   
4. Install Anaconda2:
   bash Anaconda2-4.4.0-Linux-x86_64.sh -b -p $HOME/anaconda3/envs/py2
   add " export PATH="/home/pengyong/anaconda3/envs/py2/bin:$PATH" "  to  .bashrc
   logout
   which python
   which python3
   which pip
   which pip3
   which conda
   which conda3
   which anaconda
   which anaconda3
   

5. Install some softwares by using conda3:
   anaconda3 search -t conda3 gcc
   anaconda  show  salford_systems/gcc-5
   conda install --channel https://conda.anaconda.org/salford_systems gcc-5
   

